using Test.Pattern;

namespace Test
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }
        private void Form1_Load(object sender, EventArgs e)
        {
        }



        public int _idleTimeout = 0;
        //Read timeout for input from screen and convert to time.
        private void GetSetWaitTime()
        {
        }

        //Write everything down
        private void button1_Click(object sender, EventArgs e)
        {
        }

        private Dot CreateTryWithLenght(int length)
        {
            Dot dotSequence = null;
            for (int i = 0; i < length; i++)
            {
            }

            return dotSequence;
        }
    }
}